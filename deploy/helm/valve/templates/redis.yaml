{{- if .Values.redis.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "valve.fullname" . }}-redis
  labels:
    app: {{ include "valve.fullname" . }}-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "valve.fullname" . }}-redis
  template:
    metadata:
      labels:
        app: {{ include "valve.fullname" . }}-redis
    spec:
      containers:
        - name: redis
          image: {{ .Values.redis.image }}
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "valve.fullname" . }}-redis
spec:
  selector:
    app: {{ include "valve.fullname" . }}-redis
  ports:
    - port: 6379
      targetPort: 6379
{{- end }}
