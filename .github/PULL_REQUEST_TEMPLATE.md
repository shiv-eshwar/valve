## Summary

<!-- What changed and why (1–3 sentences). -->

## Test plan

- [ ] `go test ./... -race`
- [ ] `gofmt` clean on touched `.go` files (except `pkg/gen`)
- [ ] Docs updated if behavior / API changed (`CHANGELOG.md`, `engineering.md`, or `docs/`)

## Notes

<!-- Breaking changes, follow-ups, screenshots — optional. -->
